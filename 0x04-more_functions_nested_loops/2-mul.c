#include "main.h"

/**
 * mul - a function that multiplies two multiplies two integers and returns the result
 * @a: firat number input
 * @b: second number input
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
