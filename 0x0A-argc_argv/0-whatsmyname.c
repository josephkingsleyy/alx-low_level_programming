#include <stdio.h>
#include <stdlib.h>

/**
 * A program that prints its name followed by a new line
 *
 * Return: 0 on Success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return(0);
}
